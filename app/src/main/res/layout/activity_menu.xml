<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundLight"
    android:orientation="vertical"
    tools:context="com.example.phuctruong.ezc.View.MenuActivity">

    <include
        android:id="@+id/appbar_menu"
        layout="@layout/appbar" />

    <ListView
        android:id="@+id/lsvMenu"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:divider="#e5e5e5"
        android:focusable="false"
        android:focusableInTouchMode="false"></ListView>

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="72dp"
        android:background="@color/primaryColorTransparent"
        android:gravity="center">
        <!--<ImageView-->
        <!--android:id="@+id/activity_add_icon"-->
        <!--android:layout_width="32dp"-->
        <!--android:layout_height="fill_parent"-->
        <!--android:src="@drawable/icon_add"-->
        <!--android:scaleType="fitCenter"-->
        <!--android:layout_marginRight="16dp"/>-->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:gravity="center"
            android:text="add menu"
            android:textColor="@color/textSubColor"
            android:textSize="@dimen/abc_text_size_large_material" />
    </RelativeLayout>
</LinearLayout>
